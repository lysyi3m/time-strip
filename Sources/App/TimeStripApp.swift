import SwiftUI

@main
struct TimeStripApp: App {
    var body: some Scene {
        Window("Time Strip", id: "main") {
            Text("Time Strip")
                .frame(minWidth: 360, minHeight: 200)
        }
    }
}
