name: Time Strip

options:
  bundleIdPrefix: com.mlkshkvch.timestrip
  deploymentTarget:
    macOS: "15.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.10"
    # CLI/CI builds pass CODE_SIGNING_ALLOWED=NO. For running the app + widget locally the bundles
    # must be Team-signed (ad-hoc widgets don't register with macOS's widget daemon).
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: "FXH5QLJQTJ"

targets:

  # ── UI-free core: models + time / formatting engines + zone catalog ────────
  # NO SwiftUI / WidgetKit / AppKit imports in Sources/Kit.
  TimeStripKit:
    type: framework
    platform: macOS
    sources:
      - Sources/Kit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.timestrip.kit
        GENERATE_INFOPLIST_FILE: "YES"
        DEFINES_MODULE: "YES"

  # ── Shared SwiftUI views (ribbon, palette, fixtures) ───────────────────────
  # Extracted from the widget so macOS can host canvas previews (widget-extension
  # targets can't be previewed on macOS). Imports TimeStripKit; used by app + widget.
  TimeStripUI:
    type: framework
    platform: macOS
    sources:
      - Sources/UI
    dependencies:
      - target: TimeStripKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.timestrip.ui
        GENERATE_INFOPLIST_FILE: "YES"
        DEFINES_MODULE: "YES"

  # ── Minimal host app (onboarding window); embeds the widget extension ──────
  Time Strip:
    type: application
    platform: macOS
    sources:
      - Sources/App
    dependencies:
      - target: TimeStripKit
      - target: TimeStripUI
      - target: TimeStripWidget
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.timestrip
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: Time Strip
        INFOPLIST_KEY_LSUIElement: "NO"
        # macOS requires WidgetKit extensions to be sandboxed, and an extension's sandbox must be
        # matched by its container app. (No sandbox = the widget never registers with chronod.)
        ENABLE_APP_SANDBOX: "YES"

  # ── WidgetKit extension: views, timeline provider, configuration intent ────
  TimeStripWidget:
    type: app-extension
    platform: macOS
    sources:
      - Sources/Widget
    dependencies:
      - target: TimeStripKit
      - target: TimeStripUI
      # Explicit link so appintentsmetadataprocessor extracts the WidgetConfigurationIntent
      # metadata WidgetKit reads to render the native Edit-Widget picker.
      - sdk: AppIntents.framework
    info:
      path: Config/Widget-Info.plist
      properties:
        CFBundleDisplayName: Time Strip
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.timestrip.widget
        # Mandatory for a macOS widget extension — without the App Sandbox entitlement the
        # system won't load it into the widget gallery.
        ENABLE_APP_SANDBOX: "YES"

  # ── Unit tests for the core. Not app-hosted → runner isn't sandboxed, so it
  #    can read fixtures directly. @testable import TimeStripKit. ─────────────
  TimeStripTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests
        excludes:
          - "UI/**"      # UI/ is its own target (it @needs TimeStripUI, not just Kit)
    dependencies:
      - target: TimeStripKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.timestrip.tests
        GENERATE_INFOPLIST_FILE: "YES"

  # ── Unit tests for the SwiftUI layer (public layout math). ─────────────────
  TimeStripUITests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/UI
    dependencies:
      - target: TimeStripUI
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mlkshkvch.timestrip.uitests
        GENERATE_INFOPLIST_FILE: "YES"

schemes:
  Time Strip:
    build:
      targets:
        TimeStripKit: all
        TimeStripUI: all
        TimeStripWidget: all
        Time Strip: [run, profile, archive, analyze]  # app not built during `test` → no signing to run tests
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - TimeStripTests
        - TimeStripUITests
